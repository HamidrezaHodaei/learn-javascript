const tasks = [ 
    {id: 1, title: "read a book", completed: false}
]
const addTask = (title) => {
    tasks.push({id: tasks.length + 1, title, completed: false})
}

addTask('Reading')
console.log(tasks)
